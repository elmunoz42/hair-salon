<!DOCTYPE html>
<html>
    <head>
        <title>Hair Salon</title>
        <link rel="stylesheet" href="https://maxcdn.bootstrapcdn.com/bootstrap/3.3.7/css/bootstrap.min.css">
        <link href='/css/styles.css' rel='stylesheet' type='text/css'>
    </head>
    <body>
        <div class="container">
            <div class="row">
                <div class="jumbotron" id="title">
                    <h1>Your Hair Salon</h1>
                </div>
            </div>
            <!-- row  -->
            <div class="row">
                <div class="col-md-6">
                    <div class="jumbotron">
                        <h3><a href="/clients">Clients</a></h3>
                        {% if clients is not empty %}
                                {% for client in clients %}
                                <ul>
                                <li> name: <a href="/clients/{{ client.getId }}">{{ client.getName }}</a></li>
                                <li> id: {{ client.getId }}</li>
                                </ul>
                                {% endfor %}
                        {% else %}
                        <p>No Stylists Registered.</p>
                        {% endif %}
                    </div>
                    <!-- jumbotron -->
                </div>
                <!-- left column -->
                <div class="col-md-6">
                    <div class="jumbotron">
                        <h3>Register New Stylist</h3>
                        <form action="/clients" method="post">
                            <label for="client_name">Stylist name:</label><br>
                            <input type="text" name="client_name" value=""><br>
                            <button type="submit" name="register-client">Submit</button>
                        </form>
                        <h3>Delete All</h3>
                        <form action="/clients" method="post">
                        <input name="_method" type="hidden" value="delete" required>
                        <button type="submit" class="btn button-danger" name="button">Delete All Stylists.</button>
                        </form>
                        <!-- create form -->
                    </div>
                    <!-- jumbotron -->
                </div>
                <!-- right column -->
            </div>
            <!-- row -->
        </div>
    </body>
</html>
